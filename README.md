# Traffic-Management-system
A C++ traffic routing system that models city road networks as weighted graphs and finds optimal paths using Dijkstra's algorithm. Supports Car, Bike, Train, and Plane routes with dynamic road closure/reopening. REST API backend built with C++ and the Crow framework, paired with a multi-page HTML/CSS frontend.
